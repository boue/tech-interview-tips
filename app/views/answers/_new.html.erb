<%= form_for Answer.new do |f| %>
    <%= f.text_area :content, placeholder: "Your answer ... (Markdown supported)" %>
    <%= f.hidden_field :question_id, value: @question.id %>
    <%= f.submit %>
  <% end %>
